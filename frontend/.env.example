# Leave empty in Vercel production — vercel.json rewrites /api to Render.
# Local `vite` still proxies /api to http://127.0.0.1:8000.
# VITE_API_BASE_URL=https://docparse-api.onrender.com

# Optional — only if you want the Google button (email/password works without this)
# VITE_GOOGLE_CLIENT_ID=
